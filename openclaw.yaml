exec:
  security: full
  ask: off

tools:
  elevated:
    enabled: true
    allowFrom:
      - webchat

agents:
  list:
    - id: main
      tools:
        elevated:
          enabled: true
          allowFrom:
            - webchat

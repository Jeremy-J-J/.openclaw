# 2026-04-10 工作记录

## 论文研读：DeepSeekMath & Attention Is All You Need & YOLOv11

- 完成论文 arXiv:2402.03300 (DeepSeekMath)、arXiv:1706.03762 (Attention Is All You Need)、arXiv:2410.17725 (YOLOv11) 的深度解读
- 产出保存至 `research-papers/`
- 提取并处理图表，上传至 img402.dev 图床 + 微信素材库

## 微信公众号发布最佳实践（2026-04-10 最终确认版）

### 完整链路

```
论文URL → PDF下载 → PyMuPDF提取文字+图片 → 撰写研读报告 → 上传图片到微信素材库 → 获取mmbiz URL → 手动插入图片到报告 → Markdown转HTML → 微信API上传草稿箱
```

### Step by Step 操作流程

**Step 1: 论文研读**
- 读取 `skills/paper-parse/SKILL.md` 按流程执行
- PDF下载 → PyMuPDF提取文字+图片 → 撰写研读报告
- 修改文章标题（不用"研读报告"等直白字样）
- 产出：`research-papers/{论文简称}_研读报告.md`

**Step 2: 上传图片到微信素材库**
```bash
source skills/wechat-mp-publisher/wechat.env
# 获取封面图 media_id
curl -X POST "https://api.weixin.qq.com/cgi-bin/material/add_material?access_token=${TOKEN}&type=image" \
  -F "media=@charts/fig_p1_xref155.png;type=image/png"
# 逐张上传正文中图片，获取 mmbiz URL
for f in charts/fig_*.png charts/fig_*.jpeg; do
  TYPE=$(echo $f | grep -q png && echo "image/png" || echo "image/jpeg")
  RESP=$(curl -s -X POST "https://api.weixin.qq.com/cgi-bin/material/add_material?access_token=${TOKEN}&type=image" \
    -F "media=@$f;type=$TYPE")
  echo "$f: $(echo $RESP | python3 -c 'import sys,json; print(json.load(sys.stdin).get(\"url\",\"\"))')"
done
```

**Step 3: 在研读报告中插入图片**
- 在适当位置（章节标题后、表格前后）插入图片 HTML
- 使用获取到的 mmbiz URL
- 格式：
  ```
  <p align="center"><img src="mmbiz_url" width="600"/></p>
  <p>图1：图片说明（来源：原论文 Figure X）</p>
  ```

**Step 4: Markdown 转 HTML**

转换要点（最终确认版）：
1. 所有段落统一加 `margin-top:16px`（经过多轮调试发现16px效果最好）
2. H2/H3 顶级章节标题：加 `margin-top:20px`（顶级章节匹配 `^\d+\.\s+`）
3. H3/H4 子章节（如 `#### 1.1`）：不加 margin-top
4. 顶级章节紧跟 `<hr/>` 后：也加 `margin-top:20px`（用 `prev_was_hr` 标志）
5. HTML 标签行（图片等）：直接 append，不 escape
6. 列表项 `- `：转 `<p>text</p>`，不用 `<ul><li>`
7. 表格单元格：先 `strip_md_format` 再 `inline_format`，避免双重加粗
8. LaTeX 公式：替换为文字描述，跳过 `$$`
9. 纯 `<p>` 标签即可，**不需要** `text-align:left` CSS（WeChat 默认 `<p>` 即左对齐，强行加 CSS 可能被过滤反而出问题）

**Step 5: 上传草稿箱**
```bash
curl -X POST "https://api.weixin.qq.com/cgi-bin/draft/add?access_token=${TOKEN}" \
  -H "Content-Type: application/json; charset=utf-8" \
  -d @/tmp/wechat_article.json
```

---

### 关键经验（踩坑总结）

#### 1. 图片 URL 必须用微信素材库地址
- **不能**用 img402.dev 等外部图床链接，微信会过滤
- 先用 `material/add_material` 上传图片，获取 `mmbiz.qpic.cn` 地址
- 在研读报告 markdown 中手动插入图片 HTML

#### 2. 正文中的图片必须手动插入
- PDF 提取只能得到图片文件，**不会**自动插入正文
- 需要根据论文内容，在适当位置（章节标题后、表格前后）手动插入：
  ```html
  <p align="center"><img src="mmbiz_url" width="600"/></p>
  <p>图1：图片说明（来源：原论文 Figure X）</p>
  ```
- caption 单独一行作为普通段落输出

#### 3. LaTeX 公式必须转文字描述
- 微信公众号不支持 LaTeX
- 将 `$$...$$` 公式块替换为 1-2 句话文字描述

#### 4. WeChat 不支持空行，用 CSS margin-top 替代
- WeChat 编辑器会忽略纯空行（`''`）和裸 `<br/>` 标签
- **正确方案**：所有段落统一加 `margin-top:16px`（间距一致，效果最好）

#### 5. margin-top 加在哪些位置
- **全部段落**：`margin-top:16px`（统一间距）
- 顶级章节标题（H2 全部、H3 中匹配 `^\d+\.\s+` 的）：再加 `margin-top:20px`（额外间距）
- 子章节（H4 如 `#### 1.1`）：不加额外 margin
- 顶级章节紧跟 `<hr/>` 后：加 `margin-top:20px`（用 `prev_was_hr` 标志）

#### 6. HTML 标签不能转义
- `<p align="center"><img src="...">` 不能写成 `&lt;p`
- HTML 标签行直接 append 到结果列表，**不经过 escape 函数**

#### 7. 表格单元格双重加粗问题
- 单元格内容已有 `**粗体**` 标记，先 escape 再 inline_format 会导致重复
- **解决**：先 `strip_md_format`（去掉原始 `**`）再处理

#### 8. text-align CSS 问题（重要教训）
- 最初尝试 `text-align:left`，WeChat 反而显示异常
- **教训**：直接用裸 `<p>` 标签，WeChat 默认就是左对齐，**不要加多余 CSS**

#### 9. 研读报告 markdown 中不要写 HTML 标签
- PDF 文本提取可能带入 HTML 片段，或手动写了 `<p>` 标签
- **解决**：统一用纯 Markdown 格式书写，转换脚本处理所有 HTML 生成

---

### 研读报告 Markdown 格式规范

```
标题：吸引人的修改后标题（不用"研读报告"等直白字样）
正文：
- 核心信息（论文标题、作者团队等）：直接写裸文本，不用 HTML
- 背景/目标、方法、结果、结论：直接写裸文本段落
- 顶级章节（### 1. xxx、### 2. xxx）：标准 Markdown
- 子章节（#### 1.1 xxx）：标准 Markdown
- 列表：用 Markdown "- " 格式，不用 HTML
- LaTeX 公式：转文字描述
- 图片：在适当位置手动插入 <p align="center"><img...> 和 caption 段落
- 表格：标准 Markdown 表格
- 不要写 HTML 标签在 Markdown 里
```

### Markdown → HTML 转换核心伪代码

```python
def convert_md_to_html(md):
    lines = md.split('\n')
    result = []
    table_rows = []
    first_block = True
    prev_was_hr = False

    for line in lines:
        stripped = line.strip()

        if stripped.startswith('|'):          # 表格行
            table_rows.append(stripped)
            continue

        if table_rows:
            result.extend(process_table(table_rows))
            table_rows = []
            first_block = False

        if stripped == '':
            continue

        if re.match(r'^-{3,}$', stripped):   # 分隔线
            result.append('<hr/>')
            result.append('')
            prev_was_hr = True
            first_block = False
            continue

        hm = re.match(r'^(#{1,4})\s+(.+)$', stripped)
        if hm:
            level = len(hm.group(1))
            text = hm.group(2)
            if level == 1:
                continue                              # H1 跳过
            elif level == 2:
                if text in ['核心信息', 'Part A:', 'Part B:']:
                    continue                        # 元标题跳过
                result.append(f'<p style="margin-top:20px"><strong>{text}</strong></p>')
            else:                                      # H3/H4
                is_top = bool(re.match(r'^\d+\.\s+', text))
                needs_margin = (not first_block and is_top) or (prev_was_hr and is_top)
                tag = f'<p style="margin-top:20px"><strong>{text}</strong></p>' if needs_margin else f'<p><strong>{text}</strong></p>'
                result.append(tag)
            prev_was_hr = False
            first_block = False
            continue

        if is_html_line(stripped):                 # 图片等 HTML 行
            result.append(stripped)                  # 直接保留，不 escape
            prev_was_hr = False
            continue

        if stripped.startswith('- '):             # 列表项
            text = inline_format(escape(strip_md(stripped[2:])))
            result.append(f'<p style="margin-top:16px">{text}</p>')
            prev_was_hr = False
            first_block = False
            continue

        # 普通段落：全部加 margin-top:16px
        text = inline_format(escape(strip_md(stripped)))
        result.append(f'<p style="margin-top:16px">{text}</p>')
        prev_was_hr = False
        first_block = False

    return '\n'.join(result)
```

---

### 关键依赖
- IP白名单：当前公网IP必须在微信公众号后台添加
- 凭证：`skills/wechat-mp-publisher/wechat.env` 中配置 APP_ID 和 APP_SECRET
- Python 3：用于 Markdown → HTML 转换
- PyMuPDF：用于从 PDF 提取文字和图片

### 草稿箱 Media ID 记录（今日新增）
- YOLOv11 研读报告（2026-04-10，带图）：K6zbP6fkcQN2CWU-3sv2aHIKSdbA0xE_W0VO-24a1VoP6F0Cm-nv0HL77MY-NAbn
- YOLOv11 研读报告（2026-04-10，格式调试版）：K6zbP6fkcQN2CWU-3sv2aIDJQ1et3p56i1PVSd7Z2SFjFbOyxbBZhPgdamNJfkbi
- DeepSeekMath 研读报告（2026-04-10）：K6zbP6fkcQN2CWU-3sv2aOqFBTPD3ad2e8ZC9vkzQJeZp805_A0Aib4IHqDAgAeM
- Attention Is All You Need 研读报告（2026-04-10）：K6zbP6fkcQN2CWU-3sv2aD7ioP77tWnRzGnRsaf5CTQ7K6fpV66rM2w418JKLzVa
